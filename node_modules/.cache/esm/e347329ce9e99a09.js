let express,connectDatabase;_e9e‍.w("express",[["default",["express"],function(v){express=v}]]);_e9e‍.w("./config/db",[["default",["connectDatabase"],function(v){connectDatabase=v}]]);



const app = express();


connectDatabase();


app.use(express.json({ extended: false }));

// API endpoints
/**
 * @route Get /
 * @desc Test endpoint
 */
app.get('/', (req, res) =>
    res.send('http get request sent to root api endpoint')
);

/**
 * @route POST api/users
 * @desc Register user
 */
app.post('/api/users', (req, res) => {
    _e9e‍.g.console.log(req.body);
    res.send(req.body);
});


app.listen(3000, () => console.log(`Express server running on port 3000`));
